name: flutter_wanandroid
description: A new Wanandroid Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2

  # 网络库 https://github.com/flutterchina/dio
  dio: ^5.3.2
  # Cookie https://github.com/flutterchina/dio/tree/master/plugins/cookie_manager
  dio_cookie_manager: 3.1.0

  event_bus: ^2.0.0
  shared_preferences: ^2.0.15
  synchronized: ^3.1.0
  logger: ^1.4.0
  rive: ^0.9.1 # riv动画

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true
  assets:
    - assets/anim/login_screen_character.riv

